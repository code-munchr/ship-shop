<%= form_with(model: shex_item, local: true) do |form| %>
  <% if shex_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(shex_item.errors.count, "error") %> prohibited this shex_item from being saved:</h2>

      <ul>
        <% shex_item.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :item_id %>
    <%= form.text_field :item_id %>
  </div>

  <div class="field">
    <%= form.label :shopping_experience_id %>
    <%= form.text_field :shopping_experience_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
